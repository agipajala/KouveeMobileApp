<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group
        android:checkableBehavior="single">

        <item
            android:id="@+id/dashboard"
            android:icon="@drawable/ic_home"
            android:iconTint="@color/emas"
            android:checked="true"
            android:title="Beranda"></item>

        <item
            android:id="@+id/dataMaster"
            android:title="Data Master"
            android:icon="@drawable/ic_datamaster"
            app:showAsAction="never">
        </item>

        <item
            android:id="@+id/pengadaan"
            android:icon="@drawable/ic_penjualan"
            android:title="Pengadaan"
            app:showAsAction="never">
        </item>

        <item
            android:id="@+id/penjualan"
            android:icon="@drawable/ic_penjualan"
            android:title="Penjualan"
            app:showAsAction="never">
        </item>

        <item
            android:id="@+id/keluar"
            android:icon="@drawable/ic_exit"
            android:title="Keluar"
            app:showAsAction="never">
        </item>
    </group>
</menu>